# 新闻看板系统 - 快速开始

## ✨ 项目已成功创建！

项目已在现有的 Vue 框架基础上完成搭建，采用 **Manus 工业风深色主题**设计。

## 🚀 立即启动

项目已经在运行中！访问：**http://localhost:8080**

如需重新启动：
```bash
cd d:\Users\10314\Desktop\NewsDashboard\Vue_test\vue01
npm run serve
```

## 📁 项目结构总览

```
vue01/
├── src/
│   ├── api/                      # API 接口
│   │   └── index.js             # 接口统一管理
│   ├── assets/styles/           # 样式文件
│   │   └── theme.less           # Manus 深色主题
│   ├── components/              # 组件目录
│   │   ├── common/              # 公共组件
│   │   │   ├── InputForm.vue    # 表单组件
│   │   │   ├── ResultCard.vue   # 结果卡片
│   │   │   ├── TradeTable.vue   # 数据表格
│   │   │   └── WorldMap.vue     # 地图可视化
│   │   └── layout/              # 布局组件
│   │       ├── Layout.vue       # 全局布局
│   │       └── Sidebar.vue      # 侧边栏
│   ├── router/                  # 路由配置
│   ├── store/                   # Vuex 状态管理
│   ├── utils/                   # 工具函数
│   │   └── request.js          # Axios 封装
│   ├── views/                   # 页面组件
│   │   ├── HomePage.vue         # 首页
│   │   ├── CrawlerTool.vue      # 爬虫工具
│   │   └── NlpAnalyzer.vue      # NLP 分析
│   └── App.vue                  # 根组件
└── PROJECT_DOCUMENTATION.md      # 完整文档
```

## 🎯 三大核心功能

### 1️⃣ 首页 - 贸易紧张程度看板 (`/home`)
- ✅ 世界地图可视化（ECharts）
- ✅ 连线表示贸易紧张程度（颜色编码）
- ✅ 数据表格（排序、搜索、分页）
- ✅ 详情弹窗

### 2️⃣ 贸易新闻爬虫 (`/crawler`)
- ✅ 国家选择（支持搜索）
- ✅ 时间范围筛选
- ✅ 爬取进度显示
- ✅ 新闻卡片展示
- ✅ 加载更多功能
- ✅ 跳转到 NLP 分析

### 3️⃣ NLP 分析工具 (`/nlp`)
- ✅ URL 输入模式
- ✅ 文本输入模式（支持 10000 字）
- ✅ 情感分析（正面/中性/负面）
- ✅ 置信度可视化
- ✅ 内容摘要提取
- ✅ 关键词标签

## 🎨 设计亮点

### Manus 工业风主题
- 深色系基调 (`#1a1d24`)
- 卡片式布局
- 高对比度配色
- 流畅的过渡动画
- 响应式设计

### 交互体验
- Hover 微动效果
- 加载状态反馈
- 侧边栏折叠（响应式）
- 实时时间显示
- 消息提示

## 🔌 后端对接

### 第一步：配置 API 地址

创建 `.env.development` 文件：
```env
VUE_APP_BASE_API=http://localhost:5000/api
```

### 第二步：替换模拟数据

所有接口已在 `src/api/index.js` 中定义好，只需：

1. 取消注释实际接口调用代码
2. 注释掉模拟数据代码

例如在 `HomePage.vue` 中：
```javascript
// 取消注释这行
const res = await homeApi.getTradeTension()

// 注释掉模拟数据
// setTimeout(() => { ... }, 1000)
```

### 第三步：测试接口

所有接口已包含完整的错误处理和加载状态管理。

## 📦 已安装依赖

- ✅ Vue 2.6.14
- ✅ Element UI 2.15.14
- ✅ Vue Router 3.5.1
- ✅ Vuex 3.6.2
- ✅ Axios 1.13.2
- ✅ ECharts 4.x
- ✅ Less 4.0.0

## 🛠️ 开发建议

### 1. 添加新页面
```bash
# 创建页面组件
src/views/NewPage.vue

# 在 router/index.js 添加路由
{
  path: '/new-page',
  name: 'NewPage',
  component: () => import('@/views/NewPage.vue')
}

# 在 Sidebar.vue 添加菜单项
```

### 2. 创建新组件
```bash
# 公共组件放在
src/components/common/

# 特定功能组件放在
src/components/[feature]/
```

### 3. 添加 API 接口
```javascript
// 在 src/api/index.js 添加
export const newApi = {
  getData() {
    return request({
      url: '/new-endpoint',
      method: 'get'
    })
  }
}
```

## 🐛 调试技巧

### 查看组件结构
1. 打开浏览器开发者工具
2. 安装 Vue Devtools 扩展
3. 查看组件层级和状态

### 样式调试
- 所有主题变量在 `assets/styles/theme.less`
- 使用浏览器检查元素修改样式
- 确认修改后更新到对应组件

### API 调试
- 检查 Network 标签查看请求
- `utils/request.js` 已配置拦截器
- 错误会自动显示 Message 提示

## 📝 代码规范

### 组件命名
- 组件名：`PascalCase`
- 文件名：`PascalCase.vue`

### 方法命名
- 事件处理：`handle[Action]`
- 获取数据：`fetch[Data]`
- 工具方法：`camelCase`

### 注释规范
```vue
<!--
  组件名称
  功能：组件功能描述
-->
<template>
  ...
</template>

<script>
export default {
  name: 'ComponentName',
  methods: {
    // 方法功能说明
    methodName() {
      // 实现代码
    }
  }
}
</script>
```

## 🚢 部署生产环境

### 构建项目
```bash
npm run build
```

### 部署到服务器
```bash
# 将 dist/ 目录上传到服务器
# 配置 Nginx 或其他 Web 服务器
```

### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 📚 学习资源

- Vue 2 官方文档: https://v2.vuejs.org
- Element UI 文档: https://element.eleme.io
- ECharts 文档: https://echarts.apache.org
- Vuex 文档: https://v3.vuex.vuejs.org

## ✅ 下一步

1. ✅ 项目已成功运行在 http://localhost:8080
2. 🔍 浏览三个主要页面查看功能
3. 🔌 根据后端接口文档对接真实数据
4. 🎨 根据需求调整主题样式
5. 🚀 部署到生产环境

## 💡 提示

- 当前使用**模拟数据**，便于前端开发和演示
- 所有组件都有**详细注释**，便于理解和修改
- **响应式设计**已完成，支持移动端访问
- **深色主题**变量统一管理，可轻松切换颜色方案

---

**祝开发顺利！** 🎉

如有问题，请查看 `PROJECT_DOCUMENTATION.md` 获取详细文档。
